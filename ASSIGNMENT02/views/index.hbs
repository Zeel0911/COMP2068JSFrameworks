<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>


  <div class="container my-4">
    <h1 class="text-center mb-4">{{title}}</h1>
   
    <form action="/tasks" method="POST" class="d-flex mb-4">
      <input type="text" name="title" class="form-control me-2" placeholder="Enter new task" required>
      <button class="btn btn-primary">Add Task</button>
    </form>
    {{#if tasks.length}}
      <ul class="list-group">
        {{#each tasks}}
          <li class="list-group-item d-flex justify-content-between align-items-center">
            {{this.title}}
            <span>
              <a href="/tasks/{{this._id}}/edit" class="btn btn-sm btn-warning me-2">Edit</a>
              <form action="/tasks/{{this._id}}?_method=DELETE" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure?')">Delete</button>
              </form>
            </span>
          </li>
        {{/each}}
      </ul>
    {{else}}
      <p class="text-muted text-center">No tasks yet. Add one above!</p>
    {{/if}}
  </div>

  <footer class="bg-dark text-light text-center py-3 mt-5">
    <small>&copy; {{currentYear}} To-Do List App</small>
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
